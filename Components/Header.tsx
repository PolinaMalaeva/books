import {FC} from "react";

const Header: FC = () => (
    <header className="bg-gray-400/50 text-center py-5">
        <p className="text-2xl text-gray-800">Books</p>
    </header>
);

export default Header;